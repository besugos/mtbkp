<!-- Endereço -->
<%= f.fields_for :address do |address| %>

<%= address.input :id, as: :hidden, input_html: {value: address.object.id} %>

<%
  show_name = defined?(show_name) ? show_name : false
  show_geolocation_data = defined?(show_geolocation_data) ? show_geolocation_data : false
%>

<div class="row">
  <div class="col-12">
    <%= render 'common_pages/addresses/address_fields', f: f, address: address, show_name: show_name, show_geolocation_data: show_geolocation_data %>
  </div>
</div>

<% end %>
