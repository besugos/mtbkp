<%
breadcrumbs = [
	{active: false, href: root_path, text: t('menu.initial_page')},
	{active: false, href: orders_path, text: Order.model_name.human(count: 2)},
	{active: true, href: '#', text: t('model.list')},
]
%>

<%= 
	render 'common_pages/index',
	attributes: {
		page_title: Order.model_name.human(count: 2),
		url_new_path: nil,
		policy_true: false,
		objects: @orders,
		objects_path: orders_path,
		object_name: Order.model_name.human,
		breadcrumbs: breadcrumbs,
		objects_path_export: orders_path(format: "csv", orders_grid: (params[:orders_grid].nil? ? "" : params[:orders_grid].to_enum.to_h))
	}
%>
